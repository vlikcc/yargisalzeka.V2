# .env (Development) Example
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun.
# Not: docker-compose.yml içindeki sabit değerleri (${VAR} şeklinde) bu değişkenleri kullanacak şekilde güncellerseniz, Compose otomatik olarak bu .env dosyasını okur.

# Global secrets
JWT_KEY=dev-super-secret-key-at-least-32-chars-1234567890abcd
DB_PASSWORD=postgres
GEMINI_API_KEY=replace-with-your-dev-gemini-api-key
OPENSEARCH_PASSWORD=admin

# Docker resimleri/servisleri tarafından okunan değişkenler
# PostgreSQL
POSTGRES_DB=yargisalzeka
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# JWT (issuer/audience – API Gateway URL’leri)
JWT_ISSUER=http://api-gateway:5000
JWT_AUDIENCE=http://api-gateway:5000

# Mikroservisler arası REST adresleri
SubscriptionService__BaseUrl=http://subscriptionservice:5002
SearchService__BaseUrl=http://searchservice:5004

# Sağlık kontrolleri (opsiyonel)
HealthChecks__Enabled=true

# Veri tabanı başlangıcı (dev için bazı servislerde opsiyonel)
Database__EnsureCreated=true
