# Copy this to .env and fill real values. For production, prefer copying from .env.production.example

# Global secrets
JWT_KEY=change-me-long-secure-secret-at-least-32-chars
DB_PASSWORD=postgres
GEMINI_API_KEY=replace-with-your-gemini-api-key
OPENSEARCH_PASSWORD=admin

# PostgreSQL
POSTGRES_DB=yargisalzeka
POSTGRES_USER=postgres
POSTGRES_PASSWORD=${DB_PASSWORD}

# JWT (issuer/audience – default points to API Gateway in compose network)
JWT_ISSUER=http://apigateway:5000
JWT_AUDIENCE=http://apigateway:5000

# Microservice URLs (in-network)
SubscriptionService__BaseUrl=http://subscriptionservice:5002
SearchService__BaseUrl=http://searchservice:5004

# Health checks
HealthChecks__Enabled=true

# DB init
Database__EnsureCreated=true
ASPNETCORE_ENVIRONMENT=Production
